# Strictly for testing locally and in CI. Use environments to define AWS keys in CI.

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.spatial.dialect.mysql.MySQL56SpatialDialect
spring.datasource.url=jdbc:mysql://localhost:3306/test_db
spring.datasource.username=mysql_user
spring.datasource.password=mysql_password

#aws.accessKey=
#aws.secretKey=
#aws.region=
# ^ Must be a valid region as per the AWS Java SDK
#aws.bucketName=

#rollbar.access_token=
